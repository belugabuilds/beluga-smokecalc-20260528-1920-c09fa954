import "./globals.css";
export const metadata = { title: "SmokeCalc 20260528 1920", description: "Marketplace smoke request after endpoint patch" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
